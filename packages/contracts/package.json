{
  "name": "@repo/contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/. dist/ || true",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo contracts lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
